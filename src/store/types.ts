export enum SourceStatus {
    INITIAL = 'initial',
    LOADING = 'loading',
    ERROR = 'error',
    SUCCESS = 'success',
    NOT_FOUND = 'not_fount',
}

export enum HttpStatus {
    NOT_FOUND = 404,
}
